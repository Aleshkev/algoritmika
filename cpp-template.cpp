#include <bits/stdc++.h>

using namespace std;

typedef intmax_t I;

int main() {
  // ios_base::sync_with_stdio(false);
  // cin.tie(nullptr), cout.tie(nullptr);

  I n;

#ifdef UNITEST
  cout.flush();
  system("pause");
#endif
  return 0;
}
