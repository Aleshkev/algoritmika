#include <bits/stdc++.h>

using namespace std;

typedef long long int I;
typedef pair<I, I> II;


int main()
{
    const I n = 500000 + 1;
    I m;
    cin >> m;

    vector<II> edges(m);
    for(I i = 0; i < m; ++i) {
        cin >> edges[i].first >> edges[i].second;
    }

    vector<vector<I>> graph(m)


    return 0;
}
