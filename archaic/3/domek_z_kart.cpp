#include <bits/stdc++.h>

using namespace std;

typedef int I;

int main()
{
    static I t[1 << 18];
    I m;
    cin >> m;

    I n = (1 << (m + 1));

    for(I i = 1; i < n; ++i) {
        cin >> t[i];
    }

    static I u[1 << 18];
    for(I i = 0; i < m; ++i) {
        for(I j = )
    }

    for(I i = 1; i < n; ++i) {
        cout << t[i] << ' ';
    } cout << endl;


    return 0;
}
