#include <bits/stdc++.h>

using namespace std;

typedef intmax_t I;
typedef pair<I, I> II;

int main() {
  // ios_base::sync_with_stdio(false);
  // cin.tie(nullptr);
  // cout.tie(nullptr);

  I n, k;
  cin >> n >> k;

  map<I, map<I, I>> tcx;
  tcx[0][1] = 0;

  for (I t = 0;; ++t) {
    for (auto &t_cx : tcx) {
      I c =
    }
  }

#ifdef __MINGW32__
  cout.flush();
  system("pause");
#endif
  return 0;
}
